{
  "name": "statistical-attendance",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "build": "npm run build -w web && npm run build -w server",
    "test": "npm run test -w server",
    "dev:server": "npm run dev -w server",
    "dev:web": "npm run dev -w web",
    "icons": "node scripts/make-icons.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
